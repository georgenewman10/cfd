function [yn,tn] = eulerbkwd(fh,y0,t0,h,n)


yn(1)=y0;
tn(1

for n = 1:N
    ynew = y(n) + h*f(x(n),y(n));
    work = work + 1;
    y(n+1) = y(n) + h*f(x(n+1),ynew);
    work = work + 1;
end
%plot(x,y)
%hold on
final = y(N);